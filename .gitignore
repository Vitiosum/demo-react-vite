# macOS
.DS_Store
**/.DS_Store

# Build output
# dist/ is intentionally NOT ignored — Vite builds directly to root (outDir: '.')
# index.html and assets/ at root are the committed build artifacts
build/

# Dependencies
node_modules/

# Env
.env
.env.local
