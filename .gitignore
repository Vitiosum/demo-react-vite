# macOS
.DS_Store
**/.DS_Store

# Build output
# dist/ is intentionally NOT ignored: the Vite build is committed and served
# as-is by the Clever Cloud Static runtime (CC_WEBROOT=/dist).
build/

# Dependencies
node_modules/

# Env
.env
.env.local
.gstack/
